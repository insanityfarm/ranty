# Runtime features

This section covers features that depend on the runtime rather than the core syntax alone.

The most important runtime topics for stable 4.0 are:

- attribute state and selector behavior,
- passive output formatters,
- deterministic execution through explicit seeds,
- module resolution and caching,
- the CLI and interactive REPL.
